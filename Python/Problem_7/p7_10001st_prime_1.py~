#!/usr/bin/python

def primes(up_to):
	n = 20000
	i = 
	factorslist = range(2, n)
	while i <= up_to:
		list1 = filter(lambda x: x == i or x % i, factorslist)
		i = len(list1)
		n += 2
	return list1

list2 = primes(10001)

print list2[-1]
